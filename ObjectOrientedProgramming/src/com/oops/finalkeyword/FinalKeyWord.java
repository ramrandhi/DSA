package com.oops.finalkeyword;

public class FinalKeyWord {
	
	/*
	 		when the method is final java compiler copies the bite code (from sub-routine or anything regards to internal implementation)
	 		directly with the compile code of the calling method hence eliminating the overhead associated with method call which one to 
	 		to actually resolve.
	 		
	 		Dynamic method resolution(also known as late binding):
	 		-> when the jvm determines to call which method at run time is called dynamic method resolution or late binding.
	 		Ex: method overloading(dynamic polimorphism or)
	 		
	 		Early binding:
	 		-> when the jvm determines to to call a method at compile time it is call early binding.
	 		Ex: final keyword in method which makes it call at the compiler time.
	 		
 			1. Early binding:
 			when jvm knows which method to call on compile time is called early binding.
	 */

}
